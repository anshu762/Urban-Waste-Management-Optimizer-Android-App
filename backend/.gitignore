node_modules
# Keep environment variables out of version control
.env
.env.local
# Keep build artifacts out of version control
dist/
build/
coverage/
# Keep IDE configuration files out of version control
.vscode/
.idea/
# Keep lock files for development dependency management (optional, but common)
package-lock.json
# Keep generated Prisma client out of version control
/generated/prisma
